import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.solanaAppRN.app',
  appName: 'solanaapp-reactnative',
  webDir: 'https://solana-app-delta.vercel.app/'
};

export default config;
